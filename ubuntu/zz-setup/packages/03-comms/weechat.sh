#!/usr/bin/env bash

# ---------------------------------------------------------------------------- #
# Weechat

sudo apt-get install weechat-curses # Fast, light and extensible chat client - console client
sudo apt-get install weechat-doc # Fast, light and extensible chat client - documentation
sudo apt-get install weechat-scripts # script collection for the WeeChat IRC client

sudo apt-get remove weechat-perl # Fast, light and extensible chat client - Perl plugin
sudo apt-get remove weechat-python # Fast, light and extensible chat client - Python 3 plugin
sudo apt-get remove weechat-ruby # Fast, light and extensible chat client - Ruby plugin

# ---------------------------------------------------------------------------- #
